//Access feilds by name



//Regular Expressions



//Errors

var validate = () => {
    if (washerName());
    if (genderWasher());
    if (dobW());
    if (ninW());
    if (residenceW());
    if (employeeIDW());
};

document.addEventListener('submit', validate);
